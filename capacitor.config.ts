import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'currency-market',
  webDir: 'dist/currency-market',
  bundledWebRuntime: false
};

export default config;
